# Alpha module

## Objective

Provide the minimal module artifact promoted by the published alpha Proposal.

## Boundary

This fixture does not read `.agent/state.json`, use tracker bridge commands, or
write GitHub data.  Its promotion is established only by remote-default history.
