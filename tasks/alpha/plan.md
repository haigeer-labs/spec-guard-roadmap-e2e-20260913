# Alpha module plan

1. Add `alpha` after `foundation` in the capability map.
2. Publish the alpha module artifact on the approved development branch.
3. Require human merge before running read-only promotion proof.
